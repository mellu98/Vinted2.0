services:
  - type: web
    name: gifter-pwa
    env: static
    buildCommand: npm install --legacy-peer-deps && npm run build
    publishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: NODE_VERSION
        value: 20
